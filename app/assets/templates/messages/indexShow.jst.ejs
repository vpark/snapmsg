<ul>
  <% messages.each(function (message){ %>
    <li id=<%= message.id %>><%= message.escape("title") %>
      <button class="copy_link" data-clipboard-text="<%= window.location.origin%>/#/messages/<%=message.id%>">Copy Link</button>
      <button class="edit">Edit</button>
      <button class="delete">Delete</button>
      
      <% if (message.escape("opened") == "false") { %>
       <%console.log(message.escape("opened"));%>
<button class="btn-success">Unopened</button>
        <% } else { %>
<button class="btn-warning">Opened</button>
          <% } %>
      
      
      
      
    </li>
    <br>
    <% }); %>
  </ul>